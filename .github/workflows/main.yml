name: Deploy Streamlit to HF Space

on:
  push:
    branches:
      - main  # adapte selon ta branche principale

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout main repo
      uses: actions/checkout@v3

    - name: Install Git LFS
      run: |
        sudo apt-get update
        sudo apt-get install -y git-lfs
        git lfs install

    - name: Clone HF Space repo
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
        git clone https://x-access-token:${HF_TOKEN}@huggingface.co/spaces/NicoFena/Covid_Radio_DL hf-space

    - name: Setup Git LFS in HF Space repo and track binaries
      run: |
        cd hf-space
        git lfs install
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git lfs track "*.png"
        git lfs track "*.jpg"
        git lfs track "*.jpeg"
        git lfs track "*.gif"
        git lfs track "*.webp"
        git lfs track "*.bmp"
        git add .gitattributes
        git diff --cached --quiet || git commit -m "Add git-lfs tracking for images"

    - name: Copy src/streamlit content to HF Space
      run: |
        rsync -av src/streamlit/ hf-space/

    - name: Commit and push changes to HF Space
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
        cd hf-space
        git add .
        git commit -m "Update Space from main repo" || echo "No changes to commit"
        GIT_ASKPASS=/bin/echo git push https://x-access-token:${HF_TOKEN}@huggingface.co/spaces/NicoFena/Covid_Radio_DL HEAD:main
